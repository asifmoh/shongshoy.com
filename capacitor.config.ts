import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shongshoy.www.webapp',
  appName: 'shongshoy-app',
  webDir: 'build/static/',
  bundledWebRuntime: false
};

export default config;
